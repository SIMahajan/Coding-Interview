#include <stdbool.h>
#include <stdio.h>
#include <math.h>
#include <stdbool.h>
int main()
{
    int digit = 5;
    int sum = 0;
    sum = pow(digit, 2);
    printf("%d  ", sum);
    printf("%d  ", digit*digit);
    
}